config:
  aws:region: us-east-1
  cfg:cluster-name: svk-dev-ecs-cluster
  cfg:service-name: swapi-service
  cfg:host-port: "80"
  cfg:container-port: "80"
  aws:defaultTags:
    tags:
      owner: "scott"
      purpose: "testing"
