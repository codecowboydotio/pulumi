config:
  aws:region: ap-southeast-2
  cfg:cluster-name: svk-dev-cluster
  cfg:service-name: swapi-service
  cfg:host-port: "80"
  cfg:container-port: "80"
  aws:defaultTags:
    tags:
      approver: "marc.brown"
      expirationDate: "never"
      owner: "scott.vankalken"
      purpose: "SE testing"
